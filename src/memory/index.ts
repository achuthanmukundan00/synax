/**
 * Memory module — HolographicMemory and types for `synax/memory` subpath.
 *
 * ```ts
 * import { HolographicMemory } from 'synax/memory';
 * ```
 */

export { HolographicMemory } from './HolographicMemory';
export type { MemoryEntry, MemorySearchResult, HandoffManifest } from './HolographicMemory';
