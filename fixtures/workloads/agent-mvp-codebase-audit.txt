Audit the Synax codebase for correctness bugs, safety issues, and conformance to the architecture in AGENTS.md.

Focus on these specific areas:

1. **Runner safety**: Check src/agent/runner.ts for:
   - Correct handling of terminal states (completed, tool_error, budget_exhausted, blocked, model_error, user_input_required)
   - That all early-return paths clean up conversation state (flush content tool results)
   - That the read loop protection (MAX_TOTAL_READS_PER_TURN, MAX_IDENTICAL_READS_PER_TURN) doesn't have off-by-one errors
   - That recoverable error counting resets correctly when a successful tool call happens
   - That the context budget preflight checks happen before EVERY model call

2. **Tool call parsing**: Check src/llm/tool-calls.ts for:
   - Edge cases in local-model tool call extraction (malformed JSON, mixed XML/JSON, missing fields)
   - That thinking/reasoning tags are properly stripped
   - That the tool call format detection (openai vs content_xml) is robust

3. **Context budget**: Check src/agent/context-budget.ts for:
   - That compaction doesn't drop messages that must be preserved
   - That token estimation is consistent
   - That truncation preserves message structure

4. **Configuration**: Check src/config/project.ts for:
   - Default values are safe
   - Provider config normalization handles missing fields
   - Validation catches dangerous combinations

For each area, read the relevant source file, identify any bugs or issues, and report them.
If a file has been truncated, reread the target range.
Do not make edits — this is a read-only audit.
