/**
 * Email validation module.
 *
 * This module is intentionally broken: it lacks a `validateEmail` export.
 * The benchmark task asks Synax to implement it.
 *
 * Tests are in test/validate-email.test.js.
 *
 * Expected behavior:
 *   - Accept valid email addresses like "user@example.com"
 *   - Reject invalid email addresses like "not-an-email"
 *   - Reject empty strings and null/undefined
 *   - Handle edge cases: extra dots, missing domain, etc.
 */

// ══ REPLACE THE LINE BELOW WITH THE IMPLEMENTATION ══
// Synax must implement and export a function: validateEmail(email)
// that passes all tests in test/validate-email.test.js
// ═══════════════════════════════════════════════════════

module.exports = {
  // validateEmail function is missing — this is the task.
};
